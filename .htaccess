# --- 1. CONFIGURACIÓN BÁSICA ---
# Le dice al servidor que arranque con index.html
DirectoryIndex index.html

# Seguridad y evitar conflictos de negociación
Options -Indexes -MultiViews
AddDefaultCharset UTF-8

# --- 2. REGLAS DE TRADUCCIÓN (REWRITE) ---
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # --- A. FORZAR HTTPS ---
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # --- B. QUITAR WWW ---
    RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
    RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

    # --- C. MAGIA PARA QUITAR .HTML ---
    
    # 1. Si alguien escribe manualmente ".html" en la barra, lo borramos (Redirección visual)
    RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?] [NC]
    RewriteRule ^ /%1 [R=301,L,NE]

    # 2. Si el archivo solicitado NO es una carpeta...
    RewriteCond %{REQUEST_FILENAME} !-d
    # ...y el archivo con terminación .html SÍ existe...
    RewriteCond %{REQUEST_FILENAME}.html -f
    # ...entonces muestra ese archivo internamente sin cambiar la URL
    RewriteRule ^([^\.]+)$ $1.html [NC,L]
</IfModule>